function addPartNumberOptions(partNumbers) {
        var partNumberSelect = $('#partNumber');
        partNumberSelect.empty();
        partNumberSelect.append('<option value="" selected>--select--</option>');

        partNumbers.forEach(function (partNumber) {
            partNumberSelect.append('<option value="' + partNumber + '">' + partNumber + '</option>');
        });
    }
